// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://faultypointer.github.io',
    // no base as it is for faultypointer.github.io
});
